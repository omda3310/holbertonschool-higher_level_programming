--  lists all shows contained in hbtn_0d_tvshows that have at least one genre linked.
SELECT s.title, g.genre_id FROM tv_show AS s INNER JOIN tv_show_genres AS g ON s.id = g.genre_id ORDER BY s.title, g.show_id ASC;